<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".ECKScannerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECKScanner">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.lookup.LookupActivity" />
        <activity android:name=".ui.count.CountActivity" />
        <activity android:name=".ui.transfer.TransferActivity" />
        <activity android:name=".ui.transfer.TransferListActivity" />
        <activity android:name=".ui.transfer.ReceiveTransferActivity" />
        <activity android:name=".ui.priceedit.PriceEditActivity" />
        <activity android:name=".ui.shelf.ShelfListActivity" />
        <activity android:name=".ui.shelf.ShelfScanActivity" />
    </application>
</manifest>
